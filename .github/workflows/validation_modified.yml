name: Validate CI with separate parameters
on: [push, pull_request]

jobs:
  build:
    name: Verify
    uses: apache/maven-gh-actions-shared/.github/workflows/maven-verify.yml@v4
    with:
      <<: parameters.yml