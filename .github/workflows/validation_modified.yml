name: Validate CI
on: [push, pull_request]

jobs:
   build:
      name: dotenv
      uses: sammcj/dotenv-output-action@v0.5.44
    # - name: echo hello world
    #   run: echo "hello ${{ steps.dotenv.outputs.hello }}"